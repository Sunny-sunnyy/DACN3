# Codex Review: Phase 3 Async Jobs

Decision: approved
Reviewer: Codex
Date: 2026-07-15
Implementer report: `shopping_assistant_v3/reports/phase_3_async_jobs_report.md`

## Summary

Phase 3 is approved. The worker lifecycle, API-triggered async path, stale-running recovery, sanitized failure handling, structured logs, and audit rows were verified. The implementation uses a documented daemon-thread MVP worker instead of FastAPI `BackgroundTasks`.

## Findings

No blocker or major findings.

Minor residual risk: the daemon-thread worker is local-MVP only. It has no bounded queue/backpressure and does not survive process restart. The report documents this limitation; production worker design remains a later phase concern.

## Verification

Commands run:

```bash
git status --short
```

Result: Phase 3 modified/new files plus the two pre-existing untracked files `brainstorming.md` and `prompt_session.md`.

```bash
cd shopping_assistant_v3 && uv run pytest tests/ -v
```

Result: `34 passed, 1 warning in 3.96s`.

```bash
cd shopping_assistant_v3 && uv run python -c "exec(\"import time\\nfrom fastapi.testclient import TestClient\\nfrom backend.api.main import app\\nstatuses = {}\\nwith TestClient(app) as c:\\n    for i in range(20):\\n        r = c.post('/api/chat-jobs', json={'message': f'probe {i}'})\\n        job_id = r.json()['job_id']\\n        last = None\\n        for _ in range(20):\\n            time.sleep(0.1)\\n            body = c.get(f'/api/chat-jobs/{job_id}').json()\\n            last = body['status']\\n            if last in ('completed', 'failed'):\\n                break\\n        statuses[last] = statuses.get(last, 0) + 1\\nprint(statuses)\")"
```

Result: `{'pending': 20}`.

Review artifacts generated by the probe/test run (`backend/database/app.db`, `__pycache__`, `.pytest_cache`) were removed after verification.

Re-review after implementer sanitization update:

```bash
cd shopping_assistant_v3 && uv run pytest tests/ -v
```

Result: `34 passed, 1 warning in 4.01s`.

```bash
cd shopping_assistant_v3 && uv run python -c "exec(\"import time\\nfrom fastapi.testclient import TestClient\\nfrom backend.api.main import app\\nstatuses = {}\\nwith TestClient(app) as c:\\n    for i in range(5):\\n        r = c.post('/api/chat-jobs', json={'message': f'probe {i}'})\\n        job_id = r.json()['job_id']\\n        last = None\\n        for _ in range(20):\\n            time.sleep(0.1)\\n            body = c.get(f'/api/chat-jobs/{job_id}').json()\\n            last = body['status']\\n            if last in ('completed', 'failed'):\\n                break\\n        statuses[last] = statuses.get(last, 0) + 1\\nprint(statuses)\")"
```

Result at that review point: `{'pending': 5}`. The API-triggered async blocker was still unresolved before the final fix.

Final re-review after commit-before-thread and stale-running recovery fixes:

```bash
cd shopping_assistant_v3 && uv run pytest tests/ -v
```

Result: `36 passed, 1 warning in 4.24s`.

```bash
cd shopping_assistant_v3 && uv run python -c "exec(\"import time\\nfrom fastapi.testclient import TestClient\\nfrom backend.api.main import app\\nstatuses = {}\\nwith TestClient(app) as c:\\n    for i in range(5):\\n        r = c.post('/api/chat-jobs', json={'message': f'probe {i}'})\\n        job_id = r.json()['job_id']\\n        last = None\\n        for _ in range(20):\\n            time.sleep(0.1)\\n            body = c.get(f'/api/chat-jobs/{job_id}').json()\\n            last = body['status']\\n            if last in ('completed', 'failed'):\\n                break\\n        statuses[last] = statuses.get(last, 0) + 1\\nprint(statuses)\")"
```

Result: `{'completed': 5}`.

## Scope Check

Scope stayed within Phase 3 runtime files and report. I saw no `segment4/` or `shopping_assistant_v2/` changes in this phase.

## Required Changes

None.

## Resolved Findings

- resolved: `shopping_assistant_v3/backend/worker.py:153` - the raw exception text finding was fixed. The worker now persists the sanitized message `Worker failed. Try again later.` and the tests assert sanitized user/API-visible failure text. Keep raw exception details out of API/database surfaces in future tool/model phases.
- resolved: `shopping_assistant_v3/backend/api/main.py:131` - the API-trigger race was fixed by committing the request session before starting the daemon thread. The API-level test and Codex probe both verify jobs reach `completed`.
- resolved: `shopping_assistant_v3/backend/worker.py:117` - stale `running` jobs now recover when `started_at` is older than 5 minutes or missing; fresh `running` jobs are still skipped to avoid duplicate processing.

## Approval Notes

Approved for Phase 4A planning. The next allowed implementation milestone is `Phase 4A: Mock Search/Pricing Tools`.
